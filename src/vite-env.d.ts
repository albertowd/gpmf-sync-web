/// <reference types="vite/client" />

/**
 * True only in bundles produced by a Vercel build. Replaced at build time by
 * the `define` entry in vite.config.ts.
 */
declare const __VERCEL_BUILD__: boolean;
